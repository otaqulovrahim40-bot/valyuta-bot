services:
  - type: web
    name: valyuta-bot
    runtime: python
    plan: free
    rootDir: currency_bot
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: PYTHON_VERSION
        value: 3.11.0
